# Minion Template Version

Current version: 1.27.0-1.0.0

<!-- 1.27.0: Expertise layer — SMEs, review matrix, escalation contracts (driven by field packets #5 + a follow-up packet, Copilot-authored cold review — the first packet from a different vendor's seat; vendored with evidence triage in AI/feedback/, including the recorded hierarchy-misread correction; design at docs/superpowers/specs/2026-07-03-expertise-layer-design.md) — D1 minions/smes/ starters (protocol README + charter template): SMEs are an advisory class, not roles — recommend-only, no gates, no shared-surface writes, findings-only handoff packets (no DECISION / NEXT OWNER), never in the MEMORY.md roster; SME = standing domain judgment vs RM = external investigation, with SME-findings-needing-verification routed to RM via the paired research domain. D7 discovery & selection protocol: required Consult When / Do Not Consult For charter sections (negative discovery closes off expertise creep), registry summary columns, the precedence rule (review-matrix rows always win over discovery metadata; disagreement is registry-hygiene drift and a review finding), and an optional coordinator Maturity column (advisory only). D2 minions/review-matrix.md: downstream-owned review-routing starter (change types → required reviewers); absence = charter-default routing; rows only ever ADD reviewers; skipping a matrix-required reviewer is a review finding (MEMORY.md, Execution Quality). D3 ## Escalation Contract in all seven role charters, generalizing SM's existing shape (role-specific Triggers, five-part Provide payload — evidence, design pressure, risks, options, recommendation — Route: PM default / AM architectural / Operator via existing hard-stops only, no new Operator interrupts); new self-tested esc_ok guard in governance-consistency.test.sh asserts the section across all seven. D4 RM linkage line + registry RM-domain column, no RM restructure. D5 docs/runbooks/README.md structure contract (Purpose/Prerequisites/Procedure/Validation/Rollback; explicit no-rollback statement satisfies); two hard rules (no deployment procedure without rollback, no implementation procedure without validation); DM enforces at doc-sync; three runbooks brought into compliance (add-submodule, branch-setup, issue-board-setup), two already complied. D6 coordinator mode: the coordinator repo's minions/smes/ is the shared bench, lane-safe because SMEs are advisory-only; registry + root matrix are coordinator-seat surfaces; optional projects/<key>/smes/ with local-outranks-shared. Wiring: bootstrap read order in CLAUDE.md/AGENTS.md/copilot-instructions/AI.md, INIT.md onboarding step 7, SME Consult Mode in docs/minion-prompt-modes.md, five new export-manifest rows (smes README + template + glob, review-matrix, runbooks README); CHANGELOG.d/expertise-layer.md fragment. Guardrails: no governance-token changes; full 6-file tools/tests suite green -->
<!-- 1.26.0: /handoff command (driven by the Operator's downstream session need for a durable, self-contained snapshot so a fresh session or the post-compaction context resumes cleanly; the single-writer law already mandated durability "before session end or Operator handoff" but attached no procedure, and the Session Reset template existed only as a conversational reframe; design at docs/superpowers/specs/2026-07-03-handoff-command-design.md, composes existing law — durability window, SOLE-HOLDER persistence, verdict distribution, live-state verification — into one procedure rather than inventing new rules) — commit a12fe92 (spec) + commit e0349ad (core): .claude/commands/handoff.md implements flush-then-snapshot — Phase 1 flush discharges every outstanding durability obligation first (persist SOLE-HOLDER: facts to their canonical home immediately, commit each in-flight deliverable per the durability window, batch pending DURABLE LESSONS: to their canonical homes, note-not-await running background work), Phase 2 writes minions/handoffs/<YYYY-MM-DD-HHMM>-<topic>.md (Session Reset fields, repo state, in-flight work, environment gate readings marked presumptive, pointers, memory-recall hints); minions/handoffs/README.md canonizes the surface as an ephemeral courier, not truth — committed on the active branch (Class B) because it must survive session death, but the receiving session DELETES it after pickup and commits the deletion as the consumption receipt, with a same-commit supersede rule (new /handoff for the same seat/topic replaces any prior unconsumed snapshot) and a DM staleness-sweep at the next gate's doc-sync pass; docs/minion-prompt-modes.md gains a cross-tool "Handoff Mode" entry (command-table row + section) so Codex/Copilot orchestrators run the identical protocol by prompt, the established /ship pattern; MEMORY.md gains a "Session handoffs (ephemeral)" note in the Communication Model, explicitly NOT an optional layer — always available, gated by nothing — plus a Session Reset cross-reference. Meta (this commit): export-manifest rows for .claude/commands/handoff.md and minions/handoffs/README.md (both template-replace/feature) plus the minions/handoffs/*.md glob row (downstream-owned/n/a, never exported, deleted on pickup), closing the two files the v1.25.0 manifest-completeness guard flagged as uncovered on this branch (the guard doing exactly what it was built for); CHANGELOG.d/handoff-command.md fragment. Guardrails: /handoff never merges/pushes/promotes, snapshots exclude secrets/credentials-adjacent values/personal data (same classes as the memory layer), no governance-token changes; full 6-file tools/tests suite green including manifest-completeness.test.sh now at 0 uncovered -->
<!-- 1.25.0: Upgrade ergonomics — four field-reported friction points closed (driven by a downstream field packet on the 1.21.2→1.24.0 upgrade: six releases landed clean but took hours of hand-work the tooling implied it wouldn't — classify silently missed 5 genuinely-changed files forcing a diff -rq fallback, the living-file hand merge clobbered ~4,900 charter lines on its 1.21.2 first pass and needed recovery, and Version-Specific Required Changes had been empty since 1.11.1 (owned as maintainer drift; reverse-engineering merge-blocking items per release was the single biggest time sink); packet vendored verbatim + evidence-verified triage at AI/feedback/2026-07-02-upgrade-ergonomics-{field-feedback,triage}.md, all four points CONFIRMED; what-worked-well kept on record: diff-tag + vendored-snapshot model sound, provenance blocks excellent, roster drift guard passed) — T1 (commit 18498e8): tools/tests/manifest-completeness.test.sh, the suite's 6th file — every exportable tracked file must be classified by a manifest row (globs count), closing the deeper root cause (the export/snapshot pipeline is manifest-row-driven, so unmanifested files were invisible to snapshots AND classify); manifest tools/tests/ row names it. T2 (7925438): tools/upgrade-classify.sh gains glob-aware class rows, the --repo/--from/--to git-diff completeness cross-check (any file changed <from>..<to> but absent from BOTH snapshots reports UNMANIFESTED-CHANGE and exits 4 — the silently-missed-files failure mode now loud; treat exit 4 as CI failure), and --hide-excluded (suppresses do-not-export noise, default-off back-compat); TDD, upgrade-classify.test.sh at 34 cases. T3 (1f9ee1e): Required Changes backfilled 1.12.0→1.24.0, one entry per release including negatives, + forward rule — DM writes the release's entry in the same commit as CHANGELOG assembly at the staging gate, every release, checked at the PM gate (DM/PM charters + branching-and-release-model). T4 (b42dea9): delimiter convention (Operator decision; per-release git-apply patches REJECTED as a forever maintainer artifact) — the 7 role charters + MEMORY.md carry the split-merge marker (template-verbatim above, downstream-owned below); playbook Manual-Merge Guidance gains the mechanical replace-above/preserve-below split-merge plus the merge-blocking one-time migration subsection (first upgrade crossing 1.25.0). T5 (this commit): the 1.25.0 Required Changes entry written on the feature branch dogfooding the forward rule (delimiter migration REQUIRED pointing at the migration subsection; manifest-completeness adoption REQUIRED with the may-fail-first-run-is-the-guard-working note; classify flags NOTE); export-manifest charter/MEMORY.md row notes gain split-merge pointers; CHANGELOG.d/upgrade-ergonomics.md fragment. Guardrails: no governance-token changes; full 6-file tools/tests suite green -->
<!-- 1.24.0: Model tiering — advisory role/tier guidance (driven by a Network-Inventory downstream field packet, evidence-verified in AI/feedback/2026-07-02-model-tiering-triage.md; the packet's "template says nothing about model tiers" claim was only partly true — docs/minion-prompt-modes.md:243-264 already had vendor-named pipeline-mode tier guidance — so the accepted gap was role-level guidance outside pipeline mode plus rebanding the existing vendor-named text onto vendor-neutral bands) — Part 1 (commit 1fcee26): new docs/model-tiering.md canonizes capability bands (Frontier/Mid/Economy; vendor examples as aging orientation only, never requirements), the role/activity → tier map (PM orchestrator seat / AM architecture / gate decisions and SM security review / adversarial verify at Frontier — with the packet's own wrong-HIGH-finding evidence cited as the reason adversarial-verify stays Frontier even in a cheaper session: a frontier orchestrator's sloppy diff produced a HIGH-severity "diverged duplicates" finding that was wrong, byte-identical files once a just-added header was excluded, and it shipped in a PR before correction; CM split by activity — reviewer/final-verifier at Frontier, bounded implementation at Mid via the existing coder/tester pipeline variants; DM runbooks/docs at Mid; mechanical passes at Economy), the target token profile (strong-but-occasional orchestrator over cheap-and-frequent minions), and the escalate-by-session-stakes rule; docs/minion-prompt-modes.md's existing vendor-named tier language reconciled onto the same band vocabulary. Part 2 (this commit): all 21 role launchers across the three families (.github/agents/*.agent.md, .codex/agents/*.toml, .claude/agents/*.md) gain an identical "Recommended tier: ..." advisory prose line next to the role-charter read reference, one per role, matching the doc's map exactly (om's split — Frontier for production/rollback judgment, Mid for routine runtime checks — is the closest doc-supported mapping since the doc's table does not explicitly split OM; flagged for Operator review); pure advisory prose, no model: frontmatter or Codex/Copilot functional field added or changed, Claude's seven existing pins (six opus + dm sonnet, implementing the tier map exactly) untouched and documented as that family's optional enforcement mechanism on top of the shared advisory default. Meta: docs/export-manifest.md row for the new doc (yes / template-replace / reference / PM); CHANGELOG.d/model-tiering.md fragment. Guardrails: no governance-token changes, no new functional model pins, tools/tests/governance-consistency.test.sh does not (and per the new doc, should not) check tier compliance; full tools/tests suite green -->
<!-- 1.23.0: Capability discovery & utilization (driven by a work-fork field bug: minions did not know which skills/connectors/plugin agents existed in their environment and did not use them — the gap sat upstream of docs/minion-plugin-pairings.md's "Access ≠ use": no inventory, no bootstrap read, no refresh loop, no utilization instruction in cross-tool calls; design at docs/superpowers/specs/2026-07-02-capability-discovery-design.md) — D1 minions/capabilities.md starter: downstream-owned per-repo inventory table (capability; kind skill/connector/agent; per-environment availability Claude/Codex/Copilot; status active/deferred/absent; paired roles per the pairings doc; one-line use-for) with purpose preamble, clearly-marked example rows to replace, and a Refreshing-this-file section (PM is the single writer for the surface; other minions report gaps via DURABLE LESSONS:/feedback.md, never edit directly). D2 onboarding: INIT.md step 6 enumerates each AI tool's capabilities and fills the inventory (launcher families marked deferred in the checklist enter as status deferred), operator-onboarding-checklist gains a filled-inventory completion line (yes/no + date), and the pairings doc backlinks the inventory as the record that activation condition 1 (present in environment) holds. D3 bootstrap read + obligation: the inventory joins the CLAUDE.md/AGENTS.md/.github/copilot-instructions.md session read lists and ranks in AI.md's source-of-truth order as environment truth (absence of a listed capability at call time is a silent skip, never a blocker); MEMORY.md gains a Capability Inventory subsection (Shared Rules, above Branch Coordination Plane) plus an Execution Quality review-lens bullet, and the review-stage prompts carry the same check (/ship stage-7 read-only reviewer + stage-8 cross-vendor prompts flag hand-rolled work where an inventoried capability fit; Pipeline Mode review guidance in docs/minion-prompt-modes.md); all seven role charters carry an identical obligation line — when an inventoried capability fits the task, using it within charter limits is an obligation, and hand-rolling what a listed capability already does is a review finding; docs/minion-plugin-pairings.md reframes its former use-if-available language to the obligation, scoping conditionality to absent/non-inventoried integrations. D4 refresh loop (PM-owned): re-inventory at each milestone/run start and whenever a DURABLE LESSONS:/feedback.md entry flags a capability gap, change, or friction; tool/capability observations are a named DURABLE LESSONS: category batched into inventory updates at consolidation (Completion Handoff Contract item 10 names the category and adds minions/capabilities.md to the batching destinations); PM-authored dispatch briefs distribute the relevant inventory rows to spawned minions like reviewer verdicts. D5 tools/xtool-call.sh: standing envelope line appended to every cross-tool prompt (codex + copilot, review + delegate), mode-aware — delegate carries "Enumerate your available skills/tools first and utilize any that fit the task; report which you used."; review carries a READ-ONLY-qualified variant that forbids state-changing tool calls (the review contract is read-only via codex -s read-only / copilot deny write+shell, but side-effectful MCP connectors stay invocable there); one string per mode, each defined once so the strings cannot drift; TDD argv-capture cases (per-mode presence, unqualified line absent from review argv, placement after the caller prompt, review-mode Target suffix retained), xtool suite 65/65; the delegate-mode instruction sits in PM's dispatch-brief guidance for spawned minions (minions/roles/PM.md), carried alongside the D4 inventory-row distribution. Minor sweep (same version): capabilities.md starter example rows made self-consistent with the file's own legend (the absent example row no longer claims Claude availability), and all 21 role launchers across the three families (.github/agents/, .codex/agents/, .claude/agents/) add "Read minions/capabilities.md." to the bootstrap read preamble, matching the session-bootstrap claim; deferred: fake-provider argv capture cannot distinguish arg boundaries from newlines inside prompts (future work if multi-line assertion precision is needed). Meta: export-manifest row (yes / downstream-owned / baseline / PM — the starter must land at first export because the bootstrap read depends on it; upgrades never overwrite the filled inventory) with the matching n/a-criticality exception note; CHANGELOG.d/capability-discovery.md fragment. Guardrails: the obligation never overrides charter lanes (RM recommend-only; SM/PM no product code); no governance-token changes; full tools/tests suite green -->
<!-- 1.22.1: Overlay discipline + drift guards (M-B, same branch as 1.22.0; driven by coordinator feedback triage items 2.1/2.2/3.1/3.2/4.1/4.3/6.1/6.2, design at docs/superpowers/specs/2026-07-02-overlay-discipline-design.md) — D1 Optional-Layers convention codified as a preamble in MEMORY.md's Communication Model above the Issue Mirror / Memory Recall subsections (optional layers ship default-off behind a MINION_* gate; absence of gate or tooling is a silent no-op that never blocks a workflow; every layer's canonical doc carries an Enabling It section with activation AND rollback; governance files reference layers only in gate-conditioned language; retiring an overlay = removing its doc + pointer lines, never a multi-file governance sweep). D2 multi-session paragraph in Single-Writer Durability: concurrent sessions in one repo are each the top of their own chain (direct-commit compliant); contention trigger is 2+ sessions committing overlapping files on the same branch; first-line answer is partitioning write sets (one session per feature branch; coordinator session lanes at multi-project scale), never a serialization role. D3 deferred-state records: downstream-onboarding-playbook gains the quotable DEFERRED notice for any launcher family exported but not yet active (removed on activation; baseline files are NOT deferred by default); operator-onboarding-checklist gains one activation-state line per launcher family (Copilot/Codex/Claude: active | deferred | not exported). D4 Copilot launcher-family reconciliation: .github/agents/{am,cm,sm,dm,om}.agent.md bodies realigned to the agreeing Codex/Claude behavioral reference (CM review/investigation no-edit rule and OM's OM-Test default posture restored, among other role-level drifts; pm already matched, rm reconciled in this sweep — added the "lead with options and a recommendation" clause to .codex/agents/rm.toml and .github/agents/rm.agent.md; Copilot-specific frontmatter and tool whitelists kept; Codex/Claude families untouched), plus an Instruction-File Audit Rule line: launcher bodies for the same role stay behaviorally identical across families — audit cross-family on any launcher change. D5 canonical role list: MEMORY.md's Collaboration Model roster declared the single canonical role-set enumeration, AI.md's Role Agents list defers to it, and tools/tests/governance-consistency.test.sh gains a self-tested roster-drift guard comparing the two lists in normalized form (lowercase; om-test folds into om). D6 role-set-extension checklist in the onboarding playbook: adding a role downstream touches, at minimum, five mandatory surfaces — the role charter, every launcher family in use, the downstream MEMORY.md roster, the Completion Handoff NEXT OWNER enumeration, and the AI.md Role Agents list — historical SM drift named as the precedent. D7 plan STATUS lifecycle: minions/plans/milestone-plan-template.md replaces hardcoded Status: Active with OPEN / CLOSED — COMPLETE / CLOSED — SUPERSEDED (superseded-by: <ref>) anchored to the Exit Criteria section; minions/plans/README.md documents that the top-of-file marker (not checkbox completion) is the closure signal plus the same-day supersede rule. CHANGELOG.d/overlay-discipline.md fragment. Docs + one test guard: governance-token invariants untouched, suite green including the new D5 guard -->
<!-- 1.22.0: Coordinator-mode overlay (opt-in, Optional-Layers pattern; driven by coordinator field feedback — a real multi-project coordinator fork's 15-item packet, evidence-verified triage vendored on this branch at AI/feedback/2026-07-02-coordinator-feedback{,-triage}.md) — new docs/coordinator-mode.md canonizes running one template-derived repo as a multi-project manager: When to Scale (single MEMORY.md for one project; split to projects/<key>/MEMORY.md at 2+ concurrent projects, per-project file outranks recollection for that project's context while root MEMORY.md stays global policy); Project Registry (projects/index.md with columns project key / submodule path / repo URL / default branch / PM owner / risk tier / onboarding status; PM validates PROJECT: fields and rejects unregistered keys; rows never deleted, status retired); Mail Routing (registered project → projects/<key>/mail/, coordinator/cross-project/policy → minions/mail/; lane packets carry a PROJECT: <key> header field); Concurrent Sessions (session-lane model: one session = one project lane at a time, writes confined to projects/<key>/** + the project's submodule via its own branch flow; coordinator-shared surfaces written only by the coordinator seat with lane packets as the request path — the roll-up law applied one level up; contention eliminated by partitioning + single-writer ownership, no serialization role; multiple sessions inside one lane out of scope/unsupported); Enabling It (activate: create projects/index.md + declare in the adopting repo's MEMORY.md; rollback: archive projects/ + remove declaration; absence never blocks single-project use). New docs/runbooks/add-submodule.md (submodule add → lane scaffold → registry row onboarding → PM gate packet → PM verifies scaffold before in-progress; deinit + retired for removal). docs/downstream-upgrade-playbook.md "Coordinator-mode upgrades" subsection maps copy-directly/take-template/preserve ≈ existing manifest classes and names projects//activation-state/role-additions as expected intentional divergence for upgrade-classify.sh reads. One-line pointers in INIT.md + mailbox doc only; MEMORY.md/AI.md untouched. D8 rider: registered the previously-unmanifested .github/instructions/documentation-quality.instructions.md in export-manifest (template-replace/feature/DM) plus rows for the two new docs. CHANGELOG.d/coordinator-overlay.md fragment. Docs-only: no scripts, no governance-token changes -->
<!-- 1.21.4: Public export runbook (field-derived from the live 2026-07-02 export of this template to a PUBLIC repo, github.com/deamonkai/minions-template) — new docs/runbooks/public-export.md (Operator/PM-owned) canonizes: never push canonical history to the public copy (it tracks .mm.md/AI/ and personal Operator context across every past commit) — each publish is a manifest-filtered tree committed fresh, or committed on top of the public copy's own separate shallow history; Step 1 export from a tagged canonical release via docs/export-manifest.md rows marked Initial export: yes, deliberately adding README.md (manifest classes it downstream-owned, but the public copy has no downstream project behind it and needs a landing page) with an "About This Copy" source-version + divergence-list section; Step 2 tree-wide token-based neutralization sweep (the live run's single-line MEMORY.md pass missed an Operator-specific heading parenthetical heading echoed in INIT.md and CHANGELOG.md — the tree-wide grep caught it), feedback.md resets to a clean capture-log stub, verified by zero-hit grep + no dangling heading references; Step 3 mandatory pre-push gates (export's own tools/tests/*.test.sh suite, gitleaks detect --no-git clean, forbidden files absent: .mm.md, AI/, .remember/, .superpowers/); Step 4 single commit on the public repo's main + annotated tag matching the canonical release + push + git ls-remote verify; re-publish cadence repeats steps 1-4 on top of the public main per chosen canonical release. New export-manifest.md row (template-replace / reference / PM). Rollback note: public pushes are irreversible (caching/forking), so neutralization + gitleaks are pre-push hard gates, never post-push cleanup. CHANGELOG.d/public-export-runbook.md fragment. Docs-only: no scripts, no code changes -->
<!-- 1.21.3: tea v0.14.1 compat for the issue mirror (downstream-authored by the Molloy-trading-bot team, absorbed upstream 2026-07-02 so future downstream upgrades stop re-fighting it) — tea 0.14.1 renamed the issue-body flag --body → --description (-d) and the edit-time label flag --labels → --add-labels, and `tea labels create` exits 0 on a duplicate name (creates a second same-named label instead of failing on collision). tools/issue-sync.sh Gitea backend funcs now detect the installed tea's flag names from `tea issues create --help` and prefer --description/--add-labels with --body/--labels fallback for older builds; tools/issue-board-bootstrap.sh drops blind create-and-report for query-then-skip (snapshot `tea labels list`/`gh label list` once, skip existing), making re-runs genuinely idempotent on any host; tools/tests/fixtures/make-fake-provider.sh grows a 0.14.1-faithful fake tea (rejects --body, edit rejects --labels, labels create records dups and exits 0, labels list replays); +9 issue-sync + +3 bootstrap test cases (suites at 50/20); docs/runbooks/issue-board-setup.md gains supported-tea-version header, idempotency note, and a "tea v0.14.1 compatibility notes" section. CHANGELOG.d/tea-0141-compat.md fragment. Default-off layer contract unchanged (MINION_ISSUES=on) -->
<!-- 1.21.2: Memory gate shell-profile fix (dogfooding find, 2026-07-02) — docs/runbooks/memory-recall-setup.md's persistent-setting recipe told the Operator to export MINION_MEMORY=on in ~/.zshrc; agent tool-shells are non-interactive zsh, which never sources ~/.zshrc (only ~/.zshenv), so the gate looked "on" in the Operator's interactive terminal while every agent shell saw it unset and the layer stayed silently inert. Fix: persistent zsh export now goes in ~/.zshenv (sourced by all zsh invocations); bash guidance sets both ~/.bashrc and ~/.bash_profile, or better, verify; new rule that verification must run from the agent's own fresh tool shell (echo ${MINION_MEMORY:-<unset>}), never the interactive terminal. CHANGELOG.d/memory-gate-shell-fix.md fragment. Docs-only: no scripts, no code changes -->
<!-- 1.21.1: Verdict distribution in gate briefs (downstream PM-codex truncation incident) — a gate agent's own reader-side re-read of a large raw review artifact came up truncated, and the gate proceeded on partial evidence. New rule: dispatch briefs for gate decisions must carry the reviewer verdicts explicitly (distilled verdict, conditions, severities) transcribed verbatim by the orchestrator; raw artifacts stay available as reference, never as the gate's primary input. Pairs with the v1.20.1 live-state-briefs rule as a two-sided brief-content law: mutable world facts are verified live (they age), immutable decision records are distributed verbatim (reader-side reads of large artifacts can truncate). Notes that v1.20.0's single-writer durability window already prevents half-written artifacts; this closes the reader-side case. Wired into MEMORY.md (Execution Quality, sibling bullet to the live-state rule) and minions/roles/PM.md (Single-Writer Durability: PM-authored gate briefs embed verdicts verbatim instead of directing re-reads of raw artifacts). CHANGELOG.d/verdict-distribution.md fragment. Docs-only: no scripts, no code changes -->
<!-- 1.21.0: Optional memory recall layer (Mnemoverse view layer; default-off, gated on MINION_MEMORY=on; absence of the variable or of the memory tools/API = silent no-op, nothing ever blocks) — docs/memory-recall-model.md canonizes the model: files-always-win + recall-is-input-not-authority invariants with recalled runtime facts presumptive per the live-state-briefs rule; curated writer-owned write path (only the packet's single writer calls memory_write, only at promotion moments: applied DURABLE LESSONS: items, accepted decisions/release summaries, Operator-directed remembers); orchestrator-recall-into-briefs read path so spawned minions need no MCP access; domain scheme project:<repo-name>; four excluded classes never mirrored (secrets/credentials and credentials-adjacent state, SOLE-HOLDER: facts, personal data, packet bodies/diffs/code); REST fallback with llms.txt-transcribed endpoint paths and MNEMOVERSE_API_KEY environment-only auth. docs/runbooks/memory-recall-setup.md is the OM runbook (gate-variable facilitation per-session/persistent/direnv, per-machine Mnemoverse connection, API-key handling, write→read→stats→delete smoke test, disable/rollback via memory_delete_domain). Wired into MEMORY.md (Communication Model subsection beside the Issue mirror), minions/roles/PM.md (Single-Writer Durability: writer mirrors promoted items at consolidation and folds recall into dispatch briefs with live-state verification), and AI.md (Role Agents: recall is input-not-authority, REST fallback keeps non-Claude orchestrators capable). Export-manifest rows for both new docs; CHANGELOG.d/memory-recall.md fragment. Docs-only milestone: no scripts shipped; layer inert when unset -->
<!-- 1.20.1: Live-state briefs rule (downstream field report, third distilled rule, six agent datapoints) — a deploy brief embedded a presumed runtime snapshot ("assume a flat book"); reality at execution time was 10 carried positions, and the executing agent had to guess. New rule: dispatch briefs for runtime-touching work must instruct the agent to confirm live state first, never embed a presumed runtime snapshot — a brief states what to verify, not what is true. Added to MEMORY.md Execution Quality, OM.md Guardrails ("confirm live state first" for positions/config checksums/service status before acting), and PM.md Single-Writer Durability (PM-authored runtime-touching briefs instruct verification, not embedded state) -->
<!-- 1.20.0: Single-writer durability for the comm model (driven by a downstream field report of repo/branch write contention) — spawned minions no longer commit/push; they return the Completion Handoff packet verbatim to whoever spawned them, and only the top of the spawn chain (the single writer) commits, eliminating fan-out write contention and half-written packets; coordination artifacts roll up through the chain while code/doc deliverables stay put and are committed once in place; durability window bounds risk to at most one in-flight uncommitted deliverable (writer commits each returned piece before dispatching the next stage); WRITTEN-BY: header attributes the actual author with verbatim transcription into the repo; optional DURABLE LESSONS: handoff section lets a spawned minion flag cross-cutting learnings, batched by the writer; oversized-deliverable escape valve allows write-don't-commit to disk with a return path instead of inline transcription; all seven role charters, AI.md, and Pipeline Mode generalized onto one normalized single-writer law, replacing ad hoc "direct return" language; governance drift-guard tokens updated to block reintroduction of concurrent-writer language -->
<!-- 1.19.1: issue-sync test-hardening + diagnostic fix (deferred from the v1.19.0 final review) — +14 issue-sync.test.sh cases (GitHub edit + GitHub soft-fail to mirror Gitea; hyphenated-topic recipient parse, anchored to reject role:<recipient>-* corruption; exact banner incl. em dash; --type blocker assignee; label comma-separation); behavior change: tools/issue-sync.sh backend funcs (gitea/github create+edit) now surface tea/gh stderr on a soft-fail instead of swallowing it (2>/dev/null), keeping the exit-4/no-sidecar contract. No new files; default-off layer unchanged -->
<!-- 1.19.0: Optional host-agnostic Issue/board mirror (default-off; MINION_ISSUES=on) — tools/issue-sync.sh (host/render/sync subcommands; tiered granularity --type mail|gate|blocker|pipeline|chat, where pipeline = per-run and chat = per-day; soft-fail); tools/issue-board-bootstrap.sh (idempotent label/board setup); Gitea full (tea CLI) + GitHub interface-ready (gh CLI); docs/issue-mirror-model.md (canonical model, sidecar .issue convention, Class B/downstream-owned); docs/runbooks/issue-board-setup.md (OM runbook, host-agnostic); export-manifest rows for all four files + .issue sidecar note; role-charter updates (PM/CM/DM); MEMORY.md Communication Model subsection -->
<!-- 1.18.0: 4-tier branching model (feature→dev→staging→main) with hybrid Class-A/B coordination plane — docs/branching-and-release-model.md canonizes the flow, gate ownership, and coordination rules; staging→main hard-stop is a PR on the project's VCS host opened by PM (host-agnostic: per-host recipes (Gitea/GitHub) in docs/runbooks/branch-setup.md; tea/gh optional); CHANGELOG.d/ fragment mechanism introduced (DM assembles at staging gate, no direct CHANGELOG.md edits on feature branches); new docs/runbooks/branch-setup.md (OM-owned one-time branch-protection + PR setup, host-agnostic); export-manifest rows added for branching-and-release-model.md, branch-setup.md, CHANGELOG.d/README.md, CHANGELOG.d/*.md; Class-A mainline-authoritative note added to export-manifest; governance test extended; MEMORY/AI/role-charter updates included in this milestone -->
<!-- 1.17.0: Shadow-first / dark-ship risk posture (optional, no code shipped) — new docs/risk-posture-shadow-first.md canonizing the deferred downstream pattern: 4 layers (flag-default-off zero-compute pass-through; pure comparator {MATCH,EXPECTED,REGRESSION} by exact canonical equality; empty-by-default EXPECTED_DIVERGENCES allow-list with (note_id,justification,predicate); adopt-on-MATCH + tripwire fallback counting every divergence), isolation-test-with-teeth, third-outcome adopt-a-justified-divergence, 7-piece minimal contract, explicit when-NOT. MEMORY.md Deployment Discipline opt-in pointer + export-manifest row. Distilled from Molloy-trading-bot; trading specifics excluded -->
<!-- 1.16.0: Downstream feedback (Molloy-trading-bot) review-ergonomics quick wins — SM/DM charters mandate deltas-only review output (one-line verdict, collapse passing checks); cross-tool doc recommends running BOTH vendors on security diffs + severity re-triage (codex over-rates); MEMORY.md DoD bullet for operator-facing surfaces (flag/event/metric/feature changes); worktree-pruning note (cross-tool doc + .claude/agents/README Worktree Hygiene); onboarding emphasizes charters-as-living-state. Deferred: parallel/domain-scoped review cadence, shadow-first risk posture, operator-facing-craft. Harness 'agents linger as Running' = not template -->
<!-- 1.15.0: Triaged Copilot's 25 .github prompt-evaluation findings (first Instruction-File Audit Rule application) — applied 3 clarifications to .github/instructions/documentation-quality.instructions.md (define <project-key> placeholder, tie "gap packet" to minions/mail/, concrete doc-scope examples); rejected the other 22 as thin-by-design/already-covered/governance-locked/over-flagged (no change to the 2 governance-scanned files); removed AI/thoughts.md scratch; graduated 3 open-questions into decisions.md -->
<!-- 1.14.0: xtool-call.sh review-path hardening (TDD, 53/53 green, dependency-free) — (A) `--prompt -` reads stdin instead of the literal "-" (copilot was silently running garbage reviews); (B) review envelope reports review-failed on non-zero provider rc instead of a blanket "ok"; (C) empty prompt rejected (exit 2) + empty provider output flagged review-empty-output (exit 4). Mirrors the 1.11.1 delegate hardening; surfaced dogfooding the 1.13.0 cross-tool review -->
<!-- 1.13.0: Instruction-File Audit Rule — standing convention to audit CLAUDE.md and related AI instruction/prompt files (AGENTS.md, .github/copilot-instructions.md, role charters, *-agents launchers, repo slash-command/skill prompts) whenever they change during template improve/upgrade work; manual audit (subagent+rubric) is the cross-tool baseline, built-in analyzers are shortcuts where available (Claude /claude-md-improver, Copilot /analyze-prompt); reviewed by Codex+Copilot (AGREE-WITH-NITS, applied) -->
<!-- 1.12.0: upgrade-process tooling from downstream feedback (Molloy Trading Bot) — annotated release git tags (drift detection + `git diff <tag> <tag>`); tools/upgrade-classify.sh (automated change-set classification + live-vs-snapshot divergence); governance test --root/GOV_ROOT override + ROOT banner + externalized scan allowlist; playbook .next/-optional + lighter-patch-provenance ergonomics -->
<!-- 1.11.1: hardening from downstream upgrade feedback (Molloy Trading Bot SM) — governance-consistency test detector fix (whole-file normalize + boundary-anchored patterns + self-test); xtool-call.sh delegate hardening (F2 slug sanitization, F4 failed-delegate cleanup); doc precision (copilot url note, .superpowers gitignore enumeration, upgrade-playbook divergence step + 1.11.1 entry) -->
<!-- 1.11.0: cross-tool orchestration shipped (xtool-call.sh wrapper, /second-opinion + /delegate commands, /ship cross-vendor stage, protocol doc); governance updated to autonomous-orchestration posture with three hard-stops -->

Version format for downstream repos: `<base-template-version>-<downstream-version>`

Example downstream version: `1.9.1-1.0.0`

## Release Tagging

Every template release is published as an **annotated git tag** matching its base
version (`v1.12.0`, `v1.11.1`, …) at the release commit on `main`. This lets a
downstream detect drift cheaply (`git ls-remote --tags`) and diff exactly what
changed between two releases (`git diff v1.11.1 v1.12.0`) without staging snapshots.

Release step (maintainer) — after the release commit lands on `main`:

```
git tag -a v<version> -m "minions-template v<version>" <release-commit>
git push origin v<version>
```

Tags are immutable release markers; the `Current version` line above and
`CHANGELOG.md` stay the human-readable record. See
`docs/downstream-upgrade-playbook.md` → "Detecting Upstream Drift".

## Downstream Onboarding Method

- Keep the initial export-ready template snapshot in `.minions-template/`.
- Use [docs/downstream-onboarding-playbook.md](docs/downstream-onboarding-playbook.md) and [docs/export-manifest.md](docs/export-manifest.md) to perform the first controlled export into the live downstream repo.
- Treat `INIT.md`, `MEMORY.md`, `docs/operator-onboarding-checklist.md`, and `minion-version.md` as onboarding merge files, not blind copies.
- Export `.codex/agents/` and/or `.claude/agents/` when the downstream project
  will use Codex custom agents or Claude Code subagents for minion roles.
- Exclude `.git/` and `do-not-export` files such as `.mm.md` and the `AI/` directory from the vendored snapshot.
- After the first export, bootstrap prompt modes and mailbox coordination with
  `docs/minion-prompt-modes.md`,
  `docs/project/mailbox-collaboration-model.md`, `minions/mail/README.md`, and
  `minions/mail/packet-template.md` before opening new actionable packets.
- Commit the vendored snapshot and exported live files together so later upgrades have a stable comparison base.

## Downstream Upgrade Method

- Keep the currently approved export-ready template snapshot in `.minions-template/`.
- During upgrade, stage the incoming export-ready template in a temporary path such as `.minions-template.next/`.
- `PM` owns the downstream merge/migration unless the Operator assigns another owner.
- Use [docs/downstream-upgrade-playbook.md](docs/downstream-upgrade-playbook.md) and [docs/export-manifest.md](docs/export-manifest.md) to compare:
  - old vendored template
  - new vendored template
  - live downstream files
- Only update the base-template portion of `minion-version.md` after the live files and vendored snapshot both reflect the approved upgrade.
