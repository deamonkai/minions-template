# Operator Feedback Log

Capture log for Operator corrections, preferences, and working-style
learnings across sessions (see the Feedback Capture Rule in `MEMORY.md`).
This file is read at session start by every tool; it is a capture surface,
not a source of truth — promote durable entries into `MEMORY.md` or the
relevant `minions/roles/*.md` charter and mark them `promoted`.

Entry format:

## YYYY-MM-DD — short title

- status: open | promoted -> <destination>
- What was corrected / preferred:
- Why:
- Apply:

(No entries yet — this is a fresh copy of the template.)
